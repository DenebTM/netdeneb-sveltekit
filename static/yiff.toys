<!doctype html>
<html>
<head>
	<meta charset="utf-8">
	<title>yiff.toys - lawl</title>

	<style>
		:root {
			--bgSize: 600px;
			--bgColor: #212127;

			--fontSize: 16px;
			--fontWeight: normal;
			--fontFamily: "Open Sans", sans-serif;
		}

		html, body {
			padding:0;margin:0;
			width:100%;height:100%;
			font: var(--fontWeight) var(--fontSize) var(--fontFamily);
			background-color: var(--bgColor);
		}

		body>div {
			background: transparent url("./bg-1.png") center no-repeat;
			background-size: 500px;

			position: absolute;
			height: var(--bgSize);
			width: var(--bgSize);
			top: calc(50% - (var(--bgSize) / 2));
			left: calc(50% - (var(--bgSize) / 2));

			transition: all 2s;
		}

		body>div:hover {
			background-size: var(--bgSize);
		}
	</style>
</head>
<body>
<div></div>
</body>
</html>